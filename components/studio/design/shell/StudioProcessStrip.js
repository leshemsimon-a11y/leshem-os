// components/studio/design/shell/StudioProcessStrip.js
// Clean 6C hard rollback safety stub.
// 6C introduced this component, but Clean 6B.1 stable does not use it.
// The file is intentionally inert so stale imports/build scanners cannot
// reference removed 6C labels or crash the Studio.

export default function StudioProcessStrip() {
  return null;
}
